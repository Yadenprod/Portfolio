"""
Конфигурационный файл для настройки бота
"""

# Координаты области миникарты на экране
# Нужно настроить под ваше разрешение экрана
# Используйте инструмент для определения координат (например, Paint или специальные утилиты)

# Пример для разрешения 1920x1080:
# Миникарта обычно находится в левом нижнем углу
MINIMAP_REGION = {
    'left': 20,      # Отступ слева от края экрана
    'top': 780,      # Отступ сверху (1080 - 300 = 780 для миникарты 300x300)
    'width': 300,    # Ширина миникарты
    'height': 300    # Высота миникарты
}

# Центр миникарты (относительно области захвата)
MINIMAP_CENTER = (150, 150)  # Половина от width и height

# Параметры управления
TURN_THRESHOLD = 3  # Базовый порог угла для начала поворота (в градусах) - уменьшен для более точного наведения
TURN_THRESHOLD_CLOSE = 1.5  # Порог угла когда близко к цели - для очень точного наведения на центр
CLOSE_DISTANCE_THRESHOLD = 50  # Расстояние (в пикселях), при котором считаем что близко к цели
TURN_HYSTERESIS = 1  # Гистерезис для стабилизации - уменьшен для более точного управления
MIN_DISTANCE = 1    # Минимальное расстояние до цели (в пикселях) - бот проедет через центр, не останавливаясь
CONTINUE_AFTER_TARGET_LOST = 5.0  # Время (секунды) продолжения движения после исчезновения цели, если были близко
TARGET_LOST_DISTANCE_THRESHOLD = 70  # Расстояние (в пикселях), при котором считаем что были близко к цели (увеличено для учета перекрытия иконкой)

# Параметры обнаружения цветов (HSV)
# Красный чекпоинт #c30202: RGB(195, 2, 2) -> HSV(0, 252, 195)
RED_LOWER_1 = [0, 200, 150]    # H=0-10, S=200-255, V=150-255
RED_UPPER_1 = [10, 255, 255]
RED_LOWER_2 = [170, 200, 150]  # H=170-180 (красный на границе), S=200-255, V=150-255
RED_UPPER_2 = [180, 255, 255]

GREEN_LOWER = [40, 200, 200]  # Для зеленой палочки игрока (ярко-зеленый #00f400, #00db00)
# RGB(0, 244, 0) -> HSV: H=60, S=255, V=244
# RGB(0, 219, 0) -> HSV: H=60, S=255, V=219
# Используем высокий V (200-255) чтобы исключить темно-зеленый квадрат (V=154)
GREEN_UPPER = [80, 255, 255]

# Отдельные константы для зеленого квадрата (темно-зеленый #0b9a00)
# RGB(11, 154, 0) -> HSV примерно: H=60 (зеленый в OpenCV), S=255, V=154
# Используем более узкий диапазон H (50-70) и ограничиваем V (80-180) чтобы исключить ярко-зеленую палочку игрока
GREEN_SQUARE_LOWER = [50, 200, 80]   # Темно-зеленый: H=50-70, S=200-255, V=80-180
GREEN_SQUARE_UPPER = [70, 255, 180]

BLACK_LOWER = [0, 0, 0]
BLACK_UPPER = [180, 255, 50]

# Параметры обнаружения цветных кругов (HSV)
# Желтый круг - расширенный диапазон для лучшего обнаружения
YELLOW_LOWER = [15, 50, 50]   # Расширен диапазон оттенка и снижены пороги насыщенности/яркости
YELLOW_UPPER = [35, 255, 255]

# Розовый/Маджента круг
PINK_LOWER = [140, 100, 100]
PINK_UPPER = [170, 255, 255]

# Белый круг
WHITE_LOWER = [0, 0, 200]
WHITE_UPPER = [180, 30, 255]

# Оранжевый круг #ff6800: RGB(255, 104, 0) -> HSV(12, 255, 255)
# ВАЖНО: H=11-20 чтобы НЕ пересекаться с красным (H=0-10)
# Расширенный диапазон для надежного обнаружения в разных условиях освещения
ORANGE_LOWER = [11, 100, 100]   # H=11-20, S=100-255, V=100-255 (не пересекается с красным H=0-10)
ORANGE_UPPER = [20, 255, 255]

# Задержки
SCREEN_CAPTURE_DELAY = 0.005  # Задержка между захватами экрана (секунды) - оптимизировано для 90+ FPS
ERROR_RETRY_DELAY = 0.1      # Задержка при ошибках (секунды)
TARGET_NOT_FOUND_DELAY = 0.5 # Задержка когда цель не найдена (секунды)

# Максимальное количество последовательных ошибок перед остановкой
MAX_CONSECUTIVE_ERRORS = 10  # Ошибки захвата экрана
MAX_ARROW_ERRORS = 30  # Ошибки обнаружения стрелки игрока (увеличено для большей устойчивости)

# Отладка
SAVE_DEBUG_SCREENSHOTS = True  # Сохранять скриншоты миникарты с визуализацией (включено по умолчанию)
DEBUG_SCREENSHOT_DIR = "debug_screenshots"  # Папка для сохранения скриншотов

# Название окна игры для автоматического поиска
GAME_WINDOW_TITLE = "RADMIR CRMP"  # Можно изменить на другое название окна

